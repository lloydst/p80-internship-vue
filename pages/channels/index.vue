<template>
  <div>
    <p>middleware is in place to route from ./ to the correct channel (middleware map) still have to make it react to stuff from the DB</p>
  </div>
</template>

<script>
export default {
  created () {

  }
}

/*
 * if going the raspberry pi route we could check the ip and use it as a identifier
*/

/*

 * import the events from the db
 * see if there is a "document" that should be displayed (event)
 * if so display it
 * else see if there is a meeting / client
 * if so display it else loop()
*/

/**
 * ------------
 *  IF MEETING
 * ------------
*/

/**
 * ------------
 *  IF BUILDSERVER
 * ------------
*/

/**
 * ------------
 *  IF SUPPORT
 * ------------
*/
</script>

<style>

</style>
